<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.milktea.mapper.UserMapper"><update id="deductPointsIfEnough">UPDATE user SET points=points-#{points} WHERE id=#{userId} AND points &gt;=#{points} AND deleted=0</update><update id="deductBalanceIfEnough">UPDATE user SET balance=balance-#{amount} WHERE id=#{userId} AND balance &gt;=#{amount} AND status=1 AND deleted=0</update><select id="selectOrderStats" resultType="map">SELECT COUNT(*) totalOrders,COALESCE(SUM(pay_amount),0) totalAmount FROM orders WHERE user_id=#{userId} AND status IN(1,2,3,4)</select><select id="selectInviteStats" resultType="map">SELECT COUNT(*) inviteCount FROM invite_record WHERE inviter_id=#{userId} AND status=2</select></mapper>
