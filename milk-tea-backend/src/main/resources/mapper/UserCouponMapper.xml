<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.milktea.mapper.UserCouponMapper"><select id="selectUsageByCouponId" resultType="map">SELECT uc.user_id userId,u.username,uc.order_id orderId,o.order_no orderNo,uc.use_time useTime,uc.status FROM user_coupon uc LEFT JOIN user u ON u.id=uc.user_id LEFT JOIN orders o ON o.id=uc.order_id WHERE uc.coupon_id=#{couponId} ORDER BY uc.use_time DESC,uc.create_time DESC</select><update id="consumeIfAvailable">UPDATE user_coupon SET status=1,order_id=#{orderId},use_time=NOW() WHERE id=#{userCouponId} AND user_id=#{userId} AND status=0</update><select id="selectUserCouponsWithDetail" resultType="com.milktea.dto.UserCouponDTO">SELECT uc.id,uc.user_id userId,uc.coupon_id couponId,uc.status,uc.order_id orderId,uc.use_time useTime,uc.create_time createTime,c.name,c.type,c.discount,c.min_amount minAmount,c.valid_start startTime,c.valid_end endTime,CONCAT('满',c.min_amount,'元可用') description FROM user_coupon uc LEFT JOIN coupon c ON uc.coupon_id=c.id WHERE uc.user_id=#{userId}<if test="status != null">AND uc.status=#{status}</if> ORDER BY uc.create_time DESC</select></mapper>
