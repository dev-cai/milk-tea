<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.milktea.mapper.PointsProductMapper"><update id="decrementStockIfAvailable">UPDATE points_product SET stock=stock-#{quantity},exchanged=exchanged+#{quantity} WHERE id=#{productId} AND status=1 AND deleted=0 AND stock &gt;=#{quantity}</update></mapper>
