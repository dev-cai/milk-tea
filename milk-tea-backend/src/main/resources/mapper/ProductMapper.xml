<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.milktea.mapper.ProductMapper"><update id="decrementStock">UPDATE product SET stock=stock-#{quantity} WHERE id=#{productId} AND status=1 AND deleted=0 AND stock &gt;=#{quantity}</update><update id="incrementStock">UPDATE product SET stock=stock+#{quantity} WHERE id=#{productId} AND deleted=0</update><update id="increaseSales">UPDATE product SET sales=COALESCE(sales,0)+#{quantity} WHERE id=#{productId} AND deleted=0</update></mapper>
